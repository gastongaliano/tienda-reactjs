import React from "react";


const ItemListContainer = (props) => {
    return(
        <section>
            <h1>{props.ProximamenteAbriremosTienda}</h1>
        </section>
    );
}

export default ItemListContainer;